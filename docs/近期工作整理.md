# 近期工作整理

本文档用于汇总本轮已完成的仓库接管、配置落地与扩展排查工作，方便后续继续接手。

## 1. 已落地的文件

### 1.1 项目级指令文件

已新增项目级指令文件：

- [AGENTS.md](/Users/hanhao/Documents/Gujump-author/AGENTS.md:1)

作用：

- 把用户提供的总控要求正式落到当前仓库
- 明确总目标、总控职责、故障处理、自动化要求和主线记录要求

### 1.2 扩展实例排查记录

已新增排查记录：

- [docs/扩展实例与ServiceWorker排查记录.md](/Users/hanhao/Documents/Gujump-author/docs/扩展实例与ServiceWorker排查记录.md:1)

作用：

- 记录当前仓库对应的扩展实例
- 记录 `Service Worker` 与旧实例混淆问题的排查结论
- 作为后续 Chrome 本地调试入口说明

## 2. 已完成的确认项

### 2.1 当前仓库基础入口

已确认当前仓库是一个 Chrome `Manifest V3` 扩展：

- 清单入口：[manifest.json](/Users/hanhao/Documents/Gujump-author/manifest.json:1)
- 后台入口：[background.js](/Users/hanhao/Documents/Gujump-author/background.js:1)
- 侧边栏入口：[sidepanel/sidepanel.js](/Users/hanhao/Documents/Gujump-author/sidepanel/sidepanel.js:1)
- 步骤定义：[data/step-definitions.js](/Users/hanhao/Documents/Gujump-author/data/step-definitions.js:1)

### 2.2 当前仓库说明文档入口

已确认以下文档可作为继续阅读入口：

- [README.md](/Users/hanhao/Documents/Gujump-author/README.md:1)
- [项目完整链路说明.md](/Users/hanhao/Documents/Gujump-author/项目完整链路说明.md:1)
- [项目文件结构说明.md](/Users/hanhao/Documents/Gujump-author/项目文件结构说明.md:1)

## 3. 已完成的排查动作

### 3.1 后台脚本静态检查

已完成：

- `background.js` 的 `importScripts(...)` 依赖存在性检查
- `background.js` 本身的最小语法检查
- 所有 `importScripts(...)` 子文件的批量 `node --check`

结果：

- 当前 `background.js` 引用的脚本文件都存在
- 当前后台主文件与被加载文件未发现明显语法错误

### 3.2 扩展实例与旧标签页混淆排查

已确认：

- 当前仓库对应、可正常加载的扩展实例 ID 是 `ngfikaakjalhhbkjeapmplejadcekhod`
- 之前出现 `ERR_BLOCKED_BY_CLIENT` 的扩展页实例 ID 是 `lkplekjecodnnfmoohjjjojoiibmogkj`
- 两者不是同一实例，不能混用

详细记录见：

- [docs/扩展实例与ServiceWorker排查记录.md](/Users/hanhao/Documents/Gujump-author/docs/扩展实例与ServiceWorker排查记录.md:1)

### 3.3 当前实例运行状态确认

已确认：

- `ngfika...` 对应扩展的 `Service Worker` 可以正常打开 DevTools
- 当前实例的侧边栏页面可以正常打开：
  - `chrome-extension://ngfikaakjalhhbkjeapmplejadcekhod/sidepanel/sidepanel.html`

这说明：

- 当前仓库对应的 `0.1.5` 实例在本地至少已经恢复到“可打开 UI、可连上后台 DevTools”的状态
- 之前的扩展页报错不能直接归因到当前仓库代码

## 4. 当前结论

当前最重要的结论是：

1. 当前仓库已具备明确的项目级总控指令文件。
2. 当前仓库对应的扩展实例已明确，不再应继续使用旧实例 `lkple...` 做判断。
3. 当前仓库对应实例 `ngfika...` 的后台 worker 与侧边栏页面都能打开，至少不再是“后台完全起不来”的状态。

## 5. 后续建议

后续继续排查时，建议按这个顺序：

1. 只使用 `ngfikaakjalhhbkjeapmplejadcekhod` 这个当前仓库实例。
2. 统一从扩展详情页进入：
   - `chrome://extensions/?id=ngfikaakjalhhbkjeapmplejadcekhod`
3. 统一从当前实例 UI 入口进入：
   - `chrome-extension://ngfikaakjalhhbkjeapmplejadcekhod/sidepanel/sidepanel.html`
4. 如果再次怀疑后台异常，优先查看该实例的 `Service Worker` DevTools console。
5. 如果要继续读代码主线，优先从以下文件交叉阅读：
   - [background.js](/Users/hanhao/Documents/Gujump-author/background.js:1)
   - [sidepanel/sidepanel.js](/Users/hanhao/Documents/Gujump-author/sidepanel/sidepanel.js:1)
   - [data/step-definitions.js](/Users/hanhao/Documents/Gujump-author/data/step-definitions.js:1)
   - [项目完整链路说明.md](/Users/hanhao/Documents/Gujump-author/项目完整链路说明.md:1)

## 6. 2026-05-29 主线接管补充

### 6.1 项目级最高指令确认

已确认 [AGENTS.md](/Users/hanhao/Documents/Gujump-author/AGENTS.md:1) 已写入虚拟项目最高指令文件，并包含用户原始总控指令全文归档。后续执行必须继续按该文件要求保持总控、主线记录、有限重试和自动化优先。

本轮未重复覆盖该文件，避免改动已有内容。

### 6.2 当前自动运行主线

已确认当前主线为：

1. 侧边栏 [sidepanel/sidepanel.js](/Users/hanhao/Documents/Gujump-author/sidepanel/sidepanel.js:15042) 保存当前配置并发送 `AUTO_RUN` 或 `SCHEDULE_AUTO_RUN`
2. 后台 [background/message-router.js](/Users/hanhao/Documents/Gujump-author/background/message-router.js:1774) 校验运行状态、自动运行配置和倒计时计划
3. [background/auto-run-controller.js](/Users/hanhao/Documents/Gujump-author/background/auto-run-controller.js:514) 创建自动运行 session，按轮次、尝试次数和失败分类调度
4. [background/workflow-engine.js](/Users/hanhao/Documents/Gujump-author/background/workflow-engine.js:1) 基于 `nodeStatuses` 判断当前节点、未完成节点和运行态
5. [background/steps/registry.js](/Users/hanhao/Documents/Gujump-author/background/steps/registry.js:1) 把步骤定义映射到真实执行器
6. [data/step-definitions.js](/Users/hanhao/Documents/Gujump-author/data/step-definitions.js:1) 根据普通模式、Plus 模式、支付方式、账号接入策略和手机号注册分支解析实际步骤链

普通邮箱主链路：

`open-chatgpt -> submit-signup-email -> fill-password -> fetch-signup-code -> fill-profile -> wait-registration-success -> oauth-login -> fetch-login-code -> post-login-phone-verification -> confirm-oauth -> platform-verify`

Plus PayPal 主链路：

`open-chatgpt -> submit-signup-email -> fill-password -> fetch-signup-code -> fill-profile -> plus-checkout-create -> plus-checkout-billing -> paypal-approve -> plus-checkout-return -> oauth-login -> fetch-login-code -> post-login-phone-verification -> confirm-oauth -> platform-verify`

### 6.3 本地 helper 与验证入口

已确认 Hotmail Helper 启动方式：

```bash
./start-hotmail-helper.command
python3 scripts/hotmail_helper.py
```

默认监听：

- `http://127.0.0.1:17373`
- 健康检查：`GET /health`

已确认 GPC SMS Helper 启动方式：

```bash
python3 scripts/gpc_sms_helper_macos.py
```

默认监听：

- `http://127.0.0.1:18767`
- 主要接口：`/health`、`/latest-otp`、`/otp`

### 6.4 本轮验证结果

已执行本地纯测试：

```bash
node --test tests/*.test.js
python3 tests/sub2api_error_refresh_helper_snapshot_test.py
```

结果：

- Node 测试：83 个测试全部通过
- Python 快照测试：1 个测试通过

本轮未启动 Chrome 真实扩展流程，未点击侧边栏自动运行，未调用真实邮箱、支付、接码或平台回调链路。

### 6.5 当前自动化缺口

当前仓库还缺少统一的一键本地验证入口：

- `package.json` 暂无 `scripts`
- 暂无 Makefile / CI 脚本串起语法检查、单测、helper 健康检查和扩展加载检查
- 真实注册、邮箱、支付、SUB2API / CPA、接码链路仍依赖外部页面与配置，尚无离线 mock E2E

后续自动化改造建议先补一个只跑本地纯验证的脚本，再逐步引入 mock 外部服务层，最后再做真实扩展实例的受控 E2E。
